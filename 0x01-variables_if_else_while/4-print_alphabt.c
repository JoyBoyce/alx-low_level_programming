#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * followe by a new line, execpt q and e
 * Return: Always 0
 */
int main(void)
{
	char ls;

	for (la = 'a'; la <= 'z'; la++)
	}
		if (la != 'a' && la != 'q')
			putchar(la);
	}

	putchar('\n');

	return (0);
}
